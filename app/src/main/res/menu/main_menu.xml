<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/menuMi"
        android:icon="@drawable/ic_menu_foreground"
        android:title="@string/menu_label"
        app:showAsAction="ifRoom">
        <menu>
            <item
               android:id="@+id/addMovieMi"
               android:title="@string/menu_add_movie_label"
               app:showAsAction="withText"
               />
            <item
                android:id="@+id/sortedByNameMi"
                android:title="@string/sorted_by_name"
                app:showAsAction="withText"
                />
            <item
                android:id="@+id/sortedByRateMi"
                android:title="@string/sorted_by_rate"
                app:showAsAction="withText"
                />
        </menu>
    </item>
</menu>